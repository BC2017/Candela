# Normalize all text files to LF in the repo; checkout as-is except where noted.
* text=auto eol=lf

# Windows scripts need CRLF when checked out.
*.ps1 text eol=crlf
*.bat text eol=crlf

# Binary assets — never touch.
*.png binary
*.jpg binary
*.hdr binary
*.ktx2 binary
*.spv binary
*.glb binary
