# Games built on the engine. Each is a self-contained target with its own
# committed content — clone + build + run, no downloads.
add_subdirectory(lightkeeper)
add_subdirectory(emberwake)
add_subdirectory(tumble)
